<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="gfc.dao.UsersMapper">
	<resultMap type="gfc.dto.User" id="user_resultMap">
		<!-- <result column="pw" property="password" /> -->
	</resultMap>
		
	<select id="getUserList" resultMap="user_resultMap">
		select ucode, userid, userpw, uname, acode, ukeyword from users
	</select>
	
	<select id="getUser" parameterType="java.lang.String"
		resultMap="user_resultMap">
		select ucode, userid, userpw, uname, acode, ukeyword from users where
		userid = #{userid}
	</select>
	<insert id="addUser" parameterType="gfc.dto.User">
		insert into
		users (userid, userpw, uname, acode, ukeyword) values(#{userid}, #{userpw},
		#{uname}, #{acode}, #{ukeyword})
	</insert>
	<select id="loginUser" resultType="gfc.dto.User">
		select userid, userpw, uname from users where userid=#{userid} and userpw = #{userpw}
	</select>
</mapper>